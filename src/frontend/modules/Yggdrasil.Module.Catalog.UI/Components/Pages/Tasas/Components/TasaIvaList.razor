@using Yggdrasil.Module.Catalog.UI.Services.Catalogos
@using Yggdrasil.Module.Catalog.UI.Services.Catalogos.DTOs
@inject ICatalogosApi _catalogosApi;

<MudGrid>
    <MudItem xs="12" sm="12" md="12">
        @if (_list == null)
        {
            <MudProgressCircular Color="Color.Primary" Indeterminate="true" />
        }
        else
        {
            <MudTable Class="mb-4" Items="@_list.Results"
                      Outlined="true"
                      Elevation="0"
                      LoadingProgressColor="Color.Info"
                      Loading="_loading"
                      Dense="true"
                      Bordered="true">
                <ToolBarContent>
                    <MudStack Row="true">
                        <MudTextField @bind-Value="_searchText" OnBlur="@(() => InitialData())" Label="Tasa IVA" Margin="Margin.Dense" Variant="Variant.Text" />
                    </MudStack>

                    <MudSpacer></MudSpacer>
                    <MudStack Row="true" AlignItems="AlignItems.End">
                        <MudButton Variant="Variant.Filled" StartIcon="@Icons.Material.Filled.Add" OnClick="@(() => AddClick())" Color="Color.Primary">Agregar</MudButton>
                    </MudStack>
                </ToolBarContent>
                <ColGroup>
                    <col style="width:60%" />
                    <col style="width:20%" />
                    <col style="width:10%" />
                    <col style="width:10%" />
                </ColGroup>
                <HeaderContent>
                    <MudTh>Tasa IVA</MudTh>
                    <MudTh>Valor</MudTh>
                    <MudTh>Activa</MudTh>
                    <MudTh>Acciones</MudTh>
                </HeaderContent>
                <RowTemplate>
                    <MudTd DataLabel="Tasa IVA">@context.NomTasaIva</MudTd>
                    <MudTd DataLabel="Valor">@context.ValorTasa.ToString("F2")</MudTd>
                    <MudTd DataLabel="¿Activo?">
                        <MudSwitch Value="@context.Activo" T="bool" Size="Size.Small"
                                   ValueChanged="v => ChangeActivo(context.Id,v)" Color="Color.Primary">
                        </MudSwitch>
                    </MudTd>
                    <MudTd DataLabel="Acciones">
                        <MudStack Row="true" Justify="Justify.Center">
                            <MudIconButton Size="@MudBlazor.Size.Small"
                                           Icon="@Icons.Material.Outlined.Edit"
                                           Color="Color.Primary"
                                           OnClick="() => EditClick(context.Id)" />
                            <MudIconButton Size="@MudBlazor.Size.Small"
                                           Icon="@Icons.Material.Outlined.Delete"
                                           Color="Color.Error"
                                           OnClick="() => DeleteClick(context)" />
                        </MudStack>
                    </MudTd>
                </RowTemplate>
                <NoRecordsContent>
                    <MudText>No hay registros de <strong>Tasa IVA</strong></MudText>
                </NoRecordsContent>
                <PagerContent>
                    <MudDataGridPager T="TasaIvaListItemDto" />
                </PagerContent>
            </MudTable>
        }
    </MudItem>
</MudGrid>

@code {
    private int _pageNumber = 1;
    private int _pageSize = 10;
    private int _totalCount = 0;
    private bool _loading;
    private string? _searchText;
    private PagedResultDto<TasaIvaListItemDto>? _list;
    private DialogOptions _optionsDialogs = new DialogOptions() { CloseButton = true, MaxWidth = MaxWidth.Small, FullWidth = true };

    protected override async Task OnInitializedAsync()
    {
        if (_list == null)
        {
            await InitialData();
        }
    }

    private async Task InitialData()
    {
        _loading = true;
        var result = await _catalogosApi.GetTasasIva(
            _searchText,
            _pageNumber,
            _pageSize,
            nameof(TasaIvaListItemDto.NomTasaIva),
            false
        );
        _list = result.Data;
        _loading = false;
        _totalCount = _list!.TotalCount;
        StateHasChanged();
    }

    private async Task AddClick()
    {
        var parameters = new DialogParameters { };
        var dialog = await _dialogService.ShowAsync<TasaIvaFormDialog>("Agregar Tasa IVA", parameters, _optionsDialogs);
        var result = await dialog.Result;
        if (!result!.Canceled)
        {
            await InitialData();
        }
    }

    private async Task EditClick(int id)
    {
        var parameters = new DialogParameters
        {
            { nameof(TasaIvaFormDialog.TasaIvaId), id }
        };
        var dialog = await _dialogService.ShowAsync<TasaIvaFormDialog>("Editar Tasa IVA", parameters, _optionsDialogs);
        var result = await dialog.Result;
        if (!result!.Canceled && result.Data is true)
        {
            await InitialData();
        }
    }

    private async Task DeleteClick(TasaIvaListItemDto item)
    {
        await _dialogService.ShowConfirmDialog($"¿Desea eliminar la tasa IVA {item.NomTasaIva}?", "Eliminar", async (e) =>
        {
            try
            {
                var result = await _catalogosApi.DeleteTasaIva(item.Id);
                if (result.Success)
                {
                    _snackbarService.Add(result.Message, Severity.Success);
                    await InitialData();
                }
                else
                {
                    _snackbarService.Add(result.Errors?.FirstOrDefault() ?? "Error al eliminar", Severity.Error);
                }
            }
            catch (Exception ex)
            {
                _snackbarService.Add(ex.Message, Severity.Error);
            }
        });
    }

    private async Task ChangeActivo(int id, bool active)
    {
        var result = await _catalogosApi.ChangeActiveTasaIva(id, new ChangeActiveTasaDto
        {
            Active = active
        });
        if (result.Success)
        {
            _snackbarService.Add(result.Message, Severity.Success);
            if (_list != null)
            {
                var item = _list.Results.FirstOrDefault(u => u.Id == id);
                if (item != null)
                {
                    item.Activo = active;
                }
            }
        }
        else
        {
            _snackbarService.Add(result.Errors?.FirstOrDefault() ?? "Error", Severity.Error);
        }
        StateHasChanged();
    }

    private async void PageChangedClick(int page)
    {
        _pageNumber = page;
        await InitialData();
    }
}